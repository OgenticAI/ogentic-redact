//! `ogentic-redact-core` — real-time, on-device sensitive-content redaction.
//!
//! This crate is the heart of the `ogentic-redact` library. Detection and
//! redaction logic will land in subsequent R* tickets. This skeleton
//! establishes the crate boundary and module layout.

#![forbid(unsafe_code)]
#![deny(missing_docs)]
